{
  "name": "@supplypulse/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "seed": "tsx src/data/seed.ts",
    "smoke": "tsx src/scripts/smokeTest.ts",
    "prebuild": "npm --prefix ../shared run build",
    "build": "tsc -p tsconfig.json && node scripts/copy-data.mjs",
    "pretypecheck": "npm --prefix ../shared run build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@google/genai": "^2.12.0",
    "@supplypulse/shared": "file:../shared",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.25",
    "@types/node": "^20.19.43",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4"
  }
}
